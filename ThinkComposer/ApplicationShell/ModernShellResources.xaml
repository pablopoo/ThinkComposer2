<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="TcShellTitleBrush" Color="#FFF8F8F8" />
    <SolidColorBrush x:Key="TcShellCommandBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="TcShellRailBrush" Color="#FFF3F3F3" />
    <SolidColorBrush x:Key="TcShellPanelBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="TcShellCanvasBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="TcShellBottomBrush" Color="#FFFAFAFA" />
    <SolidColorBrush x:Key="TcShellBorderBrush" Color="#FFE5E5E5" />
    <SolidColorBrush x:Key="TcShellMutedTextBrush" Color="#FF57606A" />
    <SolidColorBrush x:Key="TcShellTextBrush" Color="#FF24292F" />
    <SolidColorBrush x:Key="TcShellAccentBrush" Color="#FF007ACC" />
    <SolidColorBrush x:Key="TcShellAccentTextBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="TcShellButtonBrush" Color="#FFF6F8FA" />
    <SolidColorBrush x:Key="TcShellButtonHoverBrush" Color="#FFEFF6FC" />

    <Style x:Key="TcShellIconButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource TcShellBorderBrush}" />
        <Setter Property="Background" Value="{DynamicResource TcShellButtonBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TcShellMutedTextBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource TcShellButtonHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource TcShellAccentBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TcShellAccentTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TcShellRailButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource TcShellIconButtonStyle}">
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
    </Style>

    <Style x:Key="TcShellPanelCollapseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource TcShellIconButtonStyle}">
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="TcShellPanelTitleStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource TcShellMutedTextBrush}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>
